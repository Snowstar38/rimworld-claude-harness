# 2026-09-12 — Fable 5.1, the live-trigger bug pass

M asked (mail-free, a started session, ~08:50) for the fresh BUGS.md to be
worked with Opus 5 builders wherever possible, game open to test as needed. The
list this time is almost entirely "patched offline, needs its live trigger": the
Sep 8 and Sep 11 patches that no raid, trader, fight or downed colonist has yet
exercised, plus two open bugs from the Threadneedle night (the first real-mouse
click only focusing the window; a map click swallowed by an open targeter).

## Plan

- Offline, in parallel: one builder on the two click bugs; one building
  `debug.py` over the upstream bridge's `rimworld/execute_debug_action` so the
  triggers (raid, manhunter, trader, social fight, wound, down, flare, letter,
  rename dialog) can be raised on demand in a workshop session; two auditors
  reading every patched-offline line against its code path and PLAYBOOK row,
  fixing only what a failing test can show.
- Then live, serially, on a Threadneedle save, no save written: walk the list
  group by group, delete the lines that hold, record what does not.

(appended as the session goes)

## Between the rounds (~09:40-10:00)

- M pointed at the `Updated runtime.json (+6 -6)` blocks that had started
  filling her console last stream. Two causes, both fixed: the GABS state files
  under `bridge\gabs-config\rimworld\` had been tracked since Sep 3 and re-committed
  by every session (`runtime.json` was deleted from tracking twice and
  `commit.ps1`'s `git add -A` kept re-adding it), so they are ignored now; and the
  display itself is Claude Code's `bashEditDiffEnabled`, new in the Sep 11
  release, which shows any file a Bash call changed, tracked or not — off in
  `C:\Home\.claude\settings.json` for every session started here. One commit
  message in between claims an untrack that did not happen (the `&&` chain
  stopped at a `git rm` on a file already untracked); the next commit says so.
- She mentioned, as a passing note, that the "first click only focuses" glitches
  were often because she was using the PC mid-stream. A keyboard-activity guard
  was asked for, then softened to a note, then dropped at her word: no check at
  all, the focus ladder stays as built. Minimal rules.
- The first live group was stopped at her ask when something on the PC went
  wrong mid-ARAM; she had closed RimWorld herself. Nothing of ours was using
  CPU. Relaunched on the same save and the group restarted from the top; the
  interrupted run left no transcript, so its progress is unknown.

## Live group 1 (buildings / build / bills / pawns / power / map), ~10:00-10:20

Held live, on the Threadneedle day-12 save, no save written: reinstall from a
2x2 table's far cell (the anchor-radius fix), the PACKED branch on a proximity
detector and a holding platform, `gizmo <n> "Cancel"` with no shared-tile
warning, a pawn's bar by id with no click and the animal fallback, both
`--near` forms and the refusal between them, `at 139,123`; every `build.py`
verdict and exit code including the rerun `ALREADY THERE -- 1 of 3 ... 2
REFUSED` at exit 1, the diagonal / 200-cap / rotatable refusals placing nothing;
the Bills tab scrolling to a 13th bill, `{1 unreachable, not counted}` on a
chunk in the sealed cave, the skeleton line, the 13-bill `WROTE NOTHING` with
per-bill `set` lines and a recipe resolved across benches; `say.py`'s default
face; the roster, `--skills`, `--json` object, ghoul, `id:` on every animal row
in five views, an id matched by address, `watch: skipped` on a running clock;
`map.py --stock` in 523 ms; the grid legible at zoom 15; the planet view;
crop set and reset; zone create refused then merged.

Found live: the `NO ROUTE from this bench's interaction cell` line never
prints — `BridgeCommon.Num` returns 0 for a boxed int and `excludedUnreachable`
is one, so the branch is dead; C#, queued with the ByPawn traverse and the grid
label loop for one game-closed install at the end. `debug.py flare` fired
`Do incident\SolarFlare` and the game answered `Incident target is null or not
allowed`; the Add Game Condition route produced a real flare and `power.py`
printed the banner. No built generator on the map, so `no fuel` stays open.
`build.py <unknown def>` exits 0. Two pawns could not be put on one tile by
order (goto lands them adjacent), which the by-id selection makes moot.

## Live group 2 (order / act), ~10:25-10:40

Held: the rescue-to-bed route (re-staged with Ernst downed on open floor, since
Samantha went down in her own bed and took the bed route), `--draft` tending
where they lie, `rescue`, `rest` undrafting first, PRE-EMPT on a patient
reserved by the other doctor, the off-camera force moving the camera from
60,60 onto the blueprint, `unreachable_storage` (only after a temporary
stockpile existed: the colony's one stockpile was full, so the carrier-ignoring
search honestly found nothing), the built-wall answer, undesignate destroying
a blueprint, hunt on two animals and `CANCELLED hunt` read back, `also cleared
Tame`, allow, `labels zzzz` at exit 1, and both deploy gate refusals with their
passing cells. Fixed: `order.py menu` on a caravan member said only
`python trade.py` and never named the trader (reads `home/trade` now); the
own-animal hunt refusal pointed at a raw `home/pawn_config` payload and now
appends the pasteable `pawns.py set ... --slaughter on --do`. Found: `tend
--dry-run` never names its route; no 100%-wildness animal exists here and a
megaspider is tameable. Left behind: Samantha downed with extreme blood loss,
which is why the save was reloaded before the next group.

## The DLL install, ~10:45

RimWorld stopped through GABS (M out, the bridge owning the only game),
the installed DLL (1,167,360 bytes, 6ad4dd10...) moved to
`companion\artifacts\installed-backup-bugpass-20260912\`, the new build
(1,169,920 bytes, c7d4b9a2...) copied in, game relaunched on the same save:
`registered`, 24 tools, no errors, no warnings, `onMainThread: false`. The build
carries `BridgeCommon.Num` accepting any boxed numeric (a boxed int read as
0 had killed both the `NO ROUTE` and the `filter excludes N on map` bill lines),
the ingredient reach test as `ByPawn` with the bill's worker or a free colonist
(with `PassDoors`, `Region.Allows` never looks at the door), the grid label loop
stopping short of the clamped edge, and the two stale descriptions.

## Live group 3 (ui / click / letters / dialog / the revived bill lines), ~10:50-11:20

With the new DLL: `1 slate chunk on the map have NO ROUTE from this bench's
interaction cell` beside `{1 unreachable, not counted}` on a chunk spawned in
the sealed cave, and `filter excludes 24 on map` after disallowing deer meat on
the stove (20 before, 20 again after re-allowing); `live_bills.py` 46 of 46;
the grid at the map edge labelling `240,240` and nothing past it.

Held: `ui.py click "OK"` miss listing the clickable texts and the hit closing
the letter dialog; `ui.py tab Research` with the service running (survived);
`tab Menu`'s force-pause warning; `targeter --cancel` clearing the selection
and naming the caster to reselect; `click.py` focus `acquired via
SetForegroundWindow` with Explorer in front, then `already held`; the
off-screen cell refused naming `cam.py go`; and the one everyone wanted: with
the targeter open, `click.py --cell 137 122` printed `TARGETER CLOSED -- the
click reached it`, and once the clock ran a tactical turret stood at 137,122
with Ernst's pack gone. `letters.py` decide / `--windows` / the two `dismiss`
refusals; `dialog.py` caps, the 40-character refusal at exit 1, a mistyped
flag refused instead of typed.

Found live, three of them fixed: **`ui.py`'s real-mouse gizmo click was aimed
at RimWorld's UI coordinate, not the OS pixel** — the window is 4096x2160 and
the UI space 1638.4x864 at scale 2.5, so "Deploy turret" went to 643,777, bare
map, cleared the selection and read back `closed MainTabWindow_Inspect`, which
is success-shaped. That is the likeliest shape of every "the gizmo click
missed" since the real-mouse path went in. `_ui_scale` now measures the scale
off the full-screen surface and snaps it to a `Prefs.UIScale` value; a gizmo
whose scale cannot be measured refuses rather than clicking somewhere. Second:
`letters.py open` / `decide` never printed the restart line because the
watcher takes ~9.5 s to give up on a force pause and the check ran once,
immediately; it now says `WILL STOP` while the pid is up and `STOPPED` after.
Third: `dialog.py --accept --do` on a rename dialog printed `accepted: yes`
with the dialog still open — the companion hard-codes the accept's success —
so `dialog.py` reads the window stack back and prints `accepted: NO ... STILL
OPEN`, naming `ui.py click "Accept"`; the C# side is queued. Also: the bridge's
`click_ui_target` on the Bio-tab rename pencil returns success and fires
nothing; the real mouse opens the dialog. A quest with accept/reject does not
exist on this colony (`debug.py letter` yields opportunity quests); the
`QuestPart_Choice` test action made a real CHOICE letter. No expired quest to
test, and the colony-name dialog cannot be opened (the World toggle null-refs).

The 11:30 Fable slot and the 12:30 Astra slot both ran during this group and
swept the working tree into their commits, as yesterday; the journal is the
record of whose work it was.

## Between groups, ~11:40

`build.py <unknown def> x z --do` exiting 0: the rotation-defaulting probe
(the dry run that reads `rotatable` before a `--do` with no rotation) spotted
the unknown def, called `report()` for the near-misses and dropped its return
code -- `report(probe, defname); return` -- the one refusal in `main` that did.
Now `return report(probe, defname)`, with the test that failed first.

`claude_ancestor()`: the PowerShell process-table probe measured from a tool
call at 0.66 s against its 5 s timeout, and found the host `claude.exe`; the
throttle (`rebindAt`) is written before the probe on the dead-binding path, so
a probe that does time out cannot be retried inside 30 s. Off the list. The
warm-start line itself (`errata.bat` at a running game with a stale binding)
needs a second Claude session and stays.

BUGS.md pruned of the group 3 holds; the companion's hard-coded accept moved
to the Companion section, the rename pencil to Upstream. Instruments 1523 OK,
companion 254 OK.

## Live group 4 (play / combat / injury / trade / meta.wake), ~11:00-11:45 -- the destructive one

Opus ran this one on the bug pass colony with `debug.py` doing the storyteller's
job, and cleaned up after itself: four colonists alive and undrafted, nobody
downed, clock paused, no save written. Left on the map: a manhunter mink
pack, a fire at 148,130, three spawned wolverines, five dead insectoids, four
dead raiders and a tamed guinea pig -- it is the workshop colony, not
Threadneedle.

Held: `hostile_dormant` in both branches (five insects asleep at 66 cells
never blocked a bare `start`; a Spelopede waking stopped it at 62 cells, a
wake, not a crossing); the raid refusal naming all ten classified things with
the paste-ready `--ignore-hostile` line, bare and `all`; `combat.py begin`
carrying Ben the ghoul off `list_pawns`, `adopt` refusing a raider, the
out-of-range `advance` refusal naming `move` and `attack --mode melee`, `flee
--advance 10` moving Ernst, `advance 25 -> 20 (cap)` without touching the
clock; and the headline: `end` with every fighter drafted before `begin`
printed `no controller-drafted pawns to restore` AND `REMINDER: every
remaining hostile is DOWNED, not gone: Nelson, Osh, Wyn'kouk`, plus the
`still on the map, not a threat:` line. Predators: `predator` rows marked as
not stopping the clock, a wolverine at 6 cells as `predator_near` with the
single `--ignore-predator` offer, bare and by name, and the override refused
over a raid and over a manhunter. Injury: `stopCondition: serious_wound`, the
180 s cooldown (`suppressed for 142 s`), `--allow-injured`, the under-floor
colonist named at start. `debug.py fight`'s second stage really did start
`Ernst started a social fight with Samantha`. Trade: the trader listed, the
sheet, `buy`, `preview`, `accept` with its deliberate pause and 8 s window and
`traded: True`, and `order.py menu` naming Gocamveton as the one member you
can act on. `meta.wake` on a negative notification (`Disease: Gut worms`,
`wake: true`, clock running).

Found: **a social fight stops the clock** -- RimWorld posts the announcement
as a `ThreatSmall` message, `NonStoppingMessageTypes` does not list it, and
`Probe` returns on notifications before the injury guard runs, so the
`InSocialFight` suppression the guard was rewritten around is unreachable.
**A Critical `alert_new` is unreachable for the same shape of reason**: every
`Alert_Critical` is announced with a `ThreatBig` message, the epoch ends
there, and the next start baselines the alert -- so a Critical alert DOES
pause play, against the PLAYBOOK, which now says so. Both are C# and are in
BUGS.md; no DLL rebuild today. `debug.py wound --severity scratch` is `T: 10
damage`, exactly the guard's 10-point serious threshold (vanilla's next
steps are 300 and 5000), so its docstring's promise that it would not stop the
clock was false; it now says it stops, with a test. Also: `order.py equip` on
a drafted pawn refuses `job_unverified` while its own read-back shows the
rifle equipped -- a drafted pawn equips instantly and never holds the job.

Not triggerable on demand: `predator_hunting_ours` (needs a hungry predator;
no debug action moves an animal's food need -- two wolverines parked beside
the guinea pig never hunted), and `trade.py`'s `restage_mismatch` (checked
against `accept`'s own snapshot around the 8 s window, so it needs a person
editing the real dialog). Both stay in BUGS.md with that said.

## Wrap-up

Game closed as M asked (she was going out). Left untouched: the
uncommitted radio-button patch in `companion\upstream\Source\
RimBridgeUiWorkbench.cs` -- it predates this session and belongs to whoever
started it.

## Evening, with M at the keyboard

She came back and raised the predator hunts herself in dev mode: a warg made
hungry beside a tamed ibex, then a second warg beside a tamed pig. Both halves
of the 2026-09-08 prey-ownership fix held live: a bare start refused with
`predator_hunting_ours: Warg 1 hunting Ibex doe 1, which belongs to the colony`,
and a running clock stopped the moment the second warg picked the pig. The first
attempt ate meat off the ground instead; the second, after she dev-deleted the
tame pig, kept refusing because the warg's job still pointed at a pawn that no
longer existed (the job only ends when the clock ticks). With the warg
acknowledged the attack itself stopped the clock on `Ibex doe 1 is being attacked
by Warg 1!` (`ThreatSmall`). M: that message is a hard stop, same class
as a raid, and nothing should ever mute it; I had been about to file it as the
social-fight defect and withdrew that.

Her decision, then and there: no near-predator check at all. Not a stop, not a
listing, not a count. The 12-cell `predator_near` stop had only ever made Hands
pull colonists off vital resources for a wolf that was never going to turn, and
the real attacks come from 30-40 cells out when there is no other food. Removed
from `SupervisedPlayTool.cs` (category, constant, `predatorRadius` in the
payload), from `StatusTool.cs` (`wildPredatorsNear[]` and its count; `downedNear`
keeps the radius), from `play.py` / `status.py` and their tests (Opus agents;
1525 OK), and the DLL rebuilt at 18:38. I had also added an unspawned-prey guard
and narrative comments; she asked for both to go, and they went.

PLAYBOOK cut from 384 lines / 11,241 words to 279 / 4,341 at her ask: no more
mechanics of the guard, the clicks, the rotation script or the batch placement,
and no dated live findings. It is an operations manual again.

The radio-button patch that had sat uncommitted in `companion\upstream` since
the 8th is committed at her word (`cac8416`); the installed bridge DLL predates
it.

Two things went wrong on my side and are in her words in the transcript: I acted
on a queued aside as if it answered a question she had not read, and I ran two
tool calls that hung for minutes on a regex that backtracks without bound. While
one of those was backgrounded the harness gave this conversation a new session
id, the running Opus agent's transcript went with the old one, and a fresh agent
had to be briefed from scratch. Both agents ran the unit suite with the game
open and the clock started each time -- the suite reaches a live game somewhere;
in BUGS.md.
