# 2026-09-03, 21:58–22:15 — pre-stream bug sweep

M asked for the most impactful cheap items from `BUGS.md` and `WANTED.md`
before tonight's stream, Opus 5 builders where possible. Five Opus 5 agents on
disjoint files, ~6 minutes each in parallel; the C# one took 12. Game launched
on `Lampblack - day 39, freezer built, Lucas alive` for the live checks;
nothing saved.

## Shipped

- **`home/pawn_config {drop}` + `gear.py`** (M's own WANTED line). One
  worn, wielded or packed item, by label substring or ThingID, dropped
  directly onto the pawn's cell — not via the gear tab's job, so it works
  paused. Dry run lists every carried item with its ThingID; ambiguity and
  locked/quest-lodger/not-ours/dead are refusals with the candidates or reason.
  Watch opens the pawn's Gear tab. Live: 21/21 dry-run checks in
  `tests\live_drop.py` (fixed the script itself — `list_pawns` apparel rows
  carry no `thingId`, so it now matches by label and takes the ID from the
  tool's `item{}`), then one real drop of Finn's packed Simple meal: applied,
  Gear tab on camera, meal on the ground at 107,142, gone from the pack.
- **`home/get_temperatures`**: refusals now carry `message` (the tool's local
  `Failure()` only wrote `error`); rooms mode with no rect refuses and points
  at `home/list_rooms`.
- **`stream.py reset`** now zeroes the turn counter, overwrites the pinned
  summary with the overlay's own empty-state text, empties the feed and posts
  a pre-turn-1 status. Tested against the live overlay on 8090 and restored.
- **`letters.py` / `move.py`**: a dialog read is now three-state (dialog /
  none / `UNKNOWN`), and every caller treats unknown as not-clear; `pending()`
  is uncapped for `auto_dismiss` (`limit` keeps the newest); `decide()` matches
  the dialog's own options exactly, refuses disabled and ambiguous; `move.pos()`
  skips the dead and `goto` reports a death instead of "done".
- **`verify.check_fire`** returns UNVERIFIED when the alert read failed and
  matches `^Fire`/`on fire`, not bare substring. **`look.py`** takes its hour
  from `home/get_time`. **`watch.py`** plays in 11 s chunks with a message
  read after each, under the 13 s message lifetime. **`act.designator`**
  refuses a bare label that maps to different designators in two categories.
- **`pawns.py --name`** widens past colonists unless a narrowing flag is given
  (`--name wolf --health` now prints the wolf). **`alerts.py`** prints the
  bare body-part group behind `Unhappy nudity` — live: `Octave: legs bare`.
- PLAYBOOK: `rim.py call` vs `rim.game()`/`rim.tool()` clarified, `optionIndex`
  named; `gear.py` row added. INSTALL: pawn_config and temperatures sections,
  tree and load-test list.

DLL 779,776 → 790,016 bytes, installed; predecessor at
`artifacts\installed-backup-20260903b\HomeBridge.BridgeTools.dll.pre-drop`.
Contract test live: 705/705. BUGS.md 17 → 10 lines, WANTED 8 → 6.

## Left open, deliberately

`execute_gizmo targetId` note, `SendMessage` harness note, the
`play_until_event` baseline swallow (companion-side carry-over, not a
pre-stream change), `ui.py` slim/find contract, `ui.row_toggle` scoping.
WANTED 1 (decide a real choice letter) still wants a workshop save.

Game left running through GABS, paused on day 39 with the one unsaved drop;
`setup.py --newest Lampblack` reloads the save from disk.
