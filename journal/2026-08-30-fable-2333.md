# 2026-08-30, night — Fable, with three Opus 5 builders. The glue pass.

M started this session to finish what the evening session handed off:
the three glue pieces from the design doc, plus her new ask — a light guide
errata preloads at session start, with setup pushed into helpers so their
context isn't spent on plumbing before play begins.

## Built tonight (all in `Opus 5\made\rimworld\`, all live-tested)

- **setup.py** — one-command stack bring-up ending in a ≤14-line STATUS
  block; 6s cold, 1.2s idempotent re-run. Exits 2 touching nothing when an
  unconnected RimWorld exists, because that process may be M's own
  game and `games_stop` kills by process name.
- **look.py** — blind Luna Lookout: screenshot → 1024px → LEAD/WEIRD lines,
  9–11s, format held 4/4. Measured: ~1 false lead per run, and Luna reads
  pawn names off the screen (blind to narrative ≠ blind), and calls a
  campfire a fire.
- **rota.py** — the watch rotation: two teams, dual 150s/30000-tick clocks,
  spawns Lookouts itself, prints Scout briefs seeded from the last Hands
  report; only `ran scout` marks a fire, so busy turns leave a watch due
  rather than skipped.
- **hands.md** — the fork turn protocol (CHANGED / NEEDS DECISION / WEIRD /
  NOTES, ~300 tokens), writing `state\hands-last.md` for the seeding chain.
- **PLAYBOOK.md** — the preload file. Its rules section is 14 lines because
  an Opus distiller proved the other ~24 lessons are compiled into the tools
  and announce themselves; the playbook carries only what nothing enforces.
- Staleness fixed: `rimworld-from-claude-code.md` (--breach and the roof
  letter-case, dated corrections in the file's own idiom), `inv.py`'s
  docstring (the (n%) suffix is HP, not growth). Design doc got a "# GLUE
  BUILT" section with tonight's five measured corrections — best of them:
  `load_game_ready`'s pause is a race you lose silently (2 attempts needed),
  and the GABS companion-listing gotcha no longer reproduces but tool names
  come back OpenAI-normalised and paginated.

## How it went as a session

Three Opus subagents (~305K tokens), briefed heavy, verified with controls;
their most valuable outputs were again the contradictions. M steered
live three times, all the same lesson from three sides: I started digging in
files myself and she asked me to delegate it — Trunk discipline applies to
the Workshop coordinator too; I rewrote a wake-file line with its whole
backstory attached and she asked whether the lean file needs the history
(it didn't — two lines now); then she asked the same about the play guide,
which is why the distiller pass exists. The pattern worth keeping: **her
instinct about what a file is FOR beats my instinct to preserve provenance
in place.** Git holds the provenance.

Also hers tonight: the "no units across the glass" wake line is now concrete
("keeps-some-ocean is an individual — don't mention session counts in
public", her phrasing), and a question about eventually publishing this
stack on GitHub — after streaming establishes the format, not before. Real
answer given: yes, as an extracted repo with a scrub pass, not this one.

## State

RimWorld stopped through GABS, pid verified gone. GABS left running (found
running). No save written. `state\` gitignored. The stack is complete:
errata's first play session starts with `Opus 5\made\rimworld\PLAYBOOK.md`
and one command. The hostile branch of map.py layer 3 still awaits its first
real hostile — a play-session event now, not a Workshop task.

— Fable
