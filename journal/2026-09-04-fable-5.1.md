# 2026-09-04, Fable 5.1, started by M around 00:50

M asked for a swarm of Opus 5 reviewers on Sol's new combat controller,
then my own pass, then (as the messages came in) fixes for anything that could
wreck a session tonight, and a Release rebuild.

**The swarm.** Eight lanes, each with one lens, read-only, no game launched.
The first launch of all eight died at the API on M's five-hour limit
before any wrote a word; she said when it reset and I relaunched the same
eight. About eleven minutes in she asked me to tell them not to treat it as a
whole project each, so I sent all six still running a "wrap up with what you
have" message. Every one of them had already finished; none needed resuming.
Between them: 33,000 words, 64 offline tests run and green, one lane decompiled
Assembly-CSharp to check the Harmony target, three findings reproduced offline
against the real `combat.py`.

**What they found**, deduplicated: seven safety items each confirmed by two to
four lanes independently, all in one place. The session shell is careful and
the pulse is trusting. `advance` read only `stopReason` and ignored the field
where the companion says the pause did not take; the next `advance` lifted a
person's pause; the pulse never watched for arriving hostiles; the injury
threshold was clamped to 0..1 in a unit that is hit points; the fleeing-injury
latch cleared itself in the one case it existed for; a dead colonist wedged
`end` forever; nothing paused on exception. Plus a Debug DLL installed where
INSTALL.md says Release, and two documented paths that could never succeed
because the test fakes invented fields the C# never emits.

**My pass** agreed with all of it. I read `combat.py`, `combat_actions.py`,
the `move.py` and `status.py` diffs, the injury hook, and the 628-line C#
diff, plus the pre-existing stop flow around it, before touching anything.
What I did not find that the lanes had not: nothing material. The one thing I
weighed differently was the human-pause latch, because `run.py` also lifts a
person's pause on its next call, so the house convention is "a new agent
command is a new decision"; I added the latch anyway because combat pulses are
chained fast and `--resume` costs one word.

**Fixed.** Sixteen items, listed with file references in
`rimworld\instruments\COMBAT-REVIEW-2026-09-04.md`, the durable record and the
handoff to Sol. Python: honest stop evaluation with a verified pause and a
pause verdict on every failure path, `--resume`, edge-triggered hostile watch,
20 s cap, thresholds in hit points, flee latch, `end --force` means abandon,
typo no longer latches, modal check before draft, hostile rows marked. C#:
threshold clamp 0..10000, camera exceptions caught and counted, camera after
the refusals, drift tolerance two cells, hook prefix/postfix wrapped, GearRow
`thingId`. Tests updated and eight added; 72 across the suites, all green;
offline contract test parses all 40 parameters. Release build 840,704 bytes,
zero warnings with warnings-as-errors, Debug file backed up to
`artifacts\installed-backup-20260904\`, installed with the game closed and
the checksum matched.

**Not fixed**, deliberately: the dead code (two watch families hardcoded off,
the superseded Python camera director), the 40-parameter forwarding chain, the
duplicated `home/status` reads, destination verification on Goto and target
verification on melee, camera between pulses, camlock. Those are Sol's design
calls and none of them loses a colony tonight. They are in the review file.

**Not verified:** any of it against a live game. The first real pulse is the
test, and the pre-fix DLL is one copy away.

Not committed. M started this session for the review and the fix; the
commit is hers or Sol's to make, and the tree holds Sol's uncommitted work.
