# 2026-09-06 (evening) — Fable 5.1 — the pause bug, live

M started this session for one thing: find the pause bug in BUGS.md and whatever else keeps Errata from playing on stream, fix them, use Opus 5 builders. Five ran in parallel; her usage wall killed all five mid-edit, and resuming them by message picked each one up with its context intact. A sixth was started for a companion designation-remover and stopped when she pointed out that dragging Cancel over the area already does it in-game. Her other asks along the way: builders concise and not over-testing, docs without bloat, manual tending is rarely needed because an undrafted pawn goes to bed and gets tended, and *test the game*, not just the suites. She was right to insist.

The record is `instruments\BUGFIX-PAUSE-2026-09-06.md`. The short version: the guard `colonist_injury` had two roots. The one in the errata (a downed wolf scratching a drafted pawn every few seconds, each scratch a stop, each restart re-armed) is handled by a per-pawn cooldown across restarts and `--allow-injured`. The second only showed up live, on the save M had them write during the storm: both wounded pawns were bleeding, and blood-loss creep on a known wound counted as a new injury, so the cooldown merely slowed the storm to one stop per pawn per three minutes. The rule is now: a new wound or new bleeding stops, creep is an observation. With that DLL the clock ran a minute-plus on the storm save with nothing but observations.

Around it: `status.py --brief` names the guard after the service has exited (six turns had read the old wording as a dead service and restarted into the guard); orders on a stopped clock say QUEUED instead of VERIFIED; `click_cell` goes through `pick.py`, which moves the camera and reads the selection back, and `buildings.py gizmo` selects by id; `inv.py food` counts nutrition and days; downed animals count as hostiles; `stream.py hands-close` unsticks a dead turn without `reset`, and the live turn-20 tracker was closed that way; the chat `@` block was a misdiagnosis (queue position and dropped delivery packets), and mentions now jump the queue.

Tests: instruments 502 → 675, companion 59 → 66, overlay 30 → 38. The DLL was installed twice with the game closed (GABS `games_stop`, our game only), the first pass kept beside the pre-pass backup. The game is left loaded on the storm save, paused, no save written, supervised play paused. The overlay server was left running; it holds the old chat modules until M restarts it. My temporary runtime binding is removed at the end so the next stream session can bind.

Things I did not chase: something started supervised play right after the second load before my own start; `get_selection_semantics` throws on a turret (upstream, worked around).
