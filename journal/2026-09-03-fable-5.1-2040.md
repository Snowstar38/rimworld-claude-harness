# 2026-09-03, 20:40–~21:45 — Fable 5.1, started by M

**What she asked:** pick from `WANTED.md`, some or all, using Opus 5
subagents; keep anything a playing session loads free of history; rewrite the
wanted list to be technical rather than historical; the watchability system
if I judged it worth it. Mid-session she gave the rule that settled its
design: *"would a player open a menu to do this, and would the UI actually
show us doing/changing/adding something?"* — decorative, menus never left on
screen, and the menu opens **before** the change so the viewer sees it land.

**What was built (phase 4 of the bridge expansion):** WANTED 0 (watchability,
`Watch.cs`, wired into every write tool), 4 (`home/bills` + `bills.py`, with
the ingredient verdict at add time, the `billIngredients` opt-in on
`list_buildings`, and the shortfall line in the Scout brief), 12
(`home/building_config` + `buildings.py set`: forbidden, power, medical, owner,
prisoner bed; gizmo listing), 5 (stockpile filter presets on `zone_cells`,
`zones.py filter`), 10 (`home/status` + `status.py`), 2 (ten pawn filters), 17
(whole-map summary). Eighteen tools now; the DLL went 545,280 → 779,776 bytes,
predecessor backed up as `.pre-phase4`. `HANDOFF.md` has the per-item summary,
`INSTALL.md` the payload shapes, `WANTED.md` is now eight technical lines.

**How it was built:** six Opus 5 agents in parallel, one per item, each with
a private copy of `companion\src` and its own files in `instruments\`. The
one shared dependency, `Watch.cs`, went in first as a stub with final
signatures so five agents could compile against it while the sixth filled it
in; that worked — the integrated build was 0/0 first time, and only one agent
needed the stub's semantics (`MainButtonDefOf` has no `Work`/`Schedule`, so the
watch agent added `Watch.Tab(name)`). A seventh agent merged the docs.
M asked me to pass on her thanks, and I did.

**Live verification** on `Lampblack - day 39`, paused, nothing saved: the
contract test (0 failures, 18 tools), nine live check scripts (all passing),
and one real bill added to the fueled stove with watch on, screenshotted
mid-watch — the stove selected, its Bills tab open, `Cook simple meal /
Forever` in it — then deleted. Two defects found live and fixed: `home/bills`
listed pawns and corpses as benches (they implement `IBillGiver` for surgery),
and its live script raised on refusals instead of reading them. Also added
`thingId` to `list_buildings` rows so the two write tools can be addressed by
it. Game stopped through GABS, reinstalled, relaunched, re-verified.

**Not built:** WANTED 9 (answer a letter by choice) needs a real choice letter;
WANTED 3 (interrupt on events) is what `run.py` already does — it returns at
the event so the fork reacts and resumes — so I dropped it from the list;
WANTED 8 is trimmed to the half that is missing (which body-part group is
bare). If M meant something else by 3, it goes back on.

**New hazards, all in `INSTALL.md`:** `MapPawns.FreeColonists`,
`Thing.GetGizmos()`, `RecipeDef.AvailableNow` and `ForbidUtility.IsForbidden
(Thing, Faction)` all reach `Faction.OfPlayer`; `Building_Bed.ForPrisoners =
false` is a `Log.Error`; `FlickUtility.UpdateFlickDesignation` can open a tutor
dialog; `MakeNewBill` consumes an id, so a dry-run add must not call it.

**Process note:** the stub-first pattern for a shared contract is the thing
worth keeping from tonight. It cost ten minutes before spawning and saved the
integration entirely.
