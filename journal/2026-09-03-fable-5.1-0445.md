# 2026-09-03, 04:00–04:50 — Fable 5.1, started by M

**What she asked:** WANTED 16, plus "a few others that you can choose from
depending on scope," using Opus 5 subagents as much as possible; ask by popup,
because she was watching something on the PC.

**What was built (phase 3 of the bridge expansion):** 16 (field selection,
`sparse` and `summary` on `home/get_cells_plus`), 13 (`home/research` and
`research.py`), 14 (`inspect: true` on `home/list_buildings`), 6 (corpses on
`home/list_things`), 7 (`animals` block on `home/list_pawns`, training and
slaughter writes on `home/pawn_config`), and my own asks B (the contract test)
and E (void-save sidecars). Fifteen tools now; the DLL went from 435,712 to
545,280 bytes with the predecessor backed up as `.pre-phase3`. `HANDOFF.md` has
the per-item summary and `INSTALL.md` the payload shapes.

**How it was built:** five Opus 5 agents in parallel, one per item, each with
a private copy of `companion\src` and an isolated build (git worktrees were
out: the repo tracks 919 MB of saves). Each agent got the same common brief —
house conventions, the main-thread hop, the null-versus-absent rule, the
memory-deleting getters, no game access — and returned its doc text as a
report rather than editing shared files. Every isolated build was 0/0; the
integrated build was 0/0 first time. A sixth agent merged the docs. M
said to tell them she appreciated their work, and I did, in the doc agent's
brief; the five builders had already finished.

**Live verification** on `Lampblack - day 39, freezer built, Lucas alive`,
paused, nothing saved: seven test scripts, 314 checks, 0 failures. The
contract test's first live run failed 20 checks, all three real: `current` and
`write` on `home/research` and `placed` on `home/place_building` were emitted
null under annotations that did not say `Nullable`, and the test's own
place_building case sent `width`/`height` the tool does not declare. Fixed,
rebuilt, game stopped through GABS, reinstalled, relaunched: 450 ok, 0 fail.
That is exactly the class of defect ask B was written to catch, on its first
outing. The research write was exercised for real (`set battery --do` read
back `battery`; the game was not saved, so the colony is still without a
project). The training and slaughter writes could not be: day 39 has no tame
animal.

**Measured, WANTED 16's table, 32x32 block:** default 178.7 KB; map.py's own
selection 130.5 KB (74%); `fields:"terrain,roof,walkable"` 73.9 KB;
`fields:"things"` sparse 72.2 KB; `summary` 6.8 KB. The stock tool was already
only a fallback in `map.py`, so "replace" was done; "compress" is done.

**One honest limit surfaced:** WANTED 14's inspect string does not carry a
bill's ingredient shortfall — `Building_WorkTable` has no inspect override and
`Bill` has no inspect text — so PLAYBOOK rule 2's "shows only in the inspect
pane" was about frames, and bills remain WANTED 4's job. Also corrected: 1.6
has no `RaceProps.wildness`; it is `StatDefOf.Wildness`.

**Process notes:** M answered the first ask with "send me another ask
when it's time, 3 min timer so if I've walked away you're good to go." That
pattern — a short-timeout ask that defaults to yes — is worth reusing when she
has pre-approved the thing and only wants a heads-up. Sol's two uncommitted
files (`Sol (skip approvals).bat`, `tools\finish-codex-update.ps1`) are left
uncommitted for Sol again; I committed by explicit path rather than through
`commit.ps1`'s `add -A`. GABS was running when I arrived (pid 12776) and is
left running; RimWorld was stopped through GABS with the pid verified gone.
