# Day 60–67 bug pass — 2026-09-06

Scope: the 40-turn report in BUGS.md for Lampblack day 60 → 67. Five parallel builders; concurrent unrelated workspace edits were preserved. No game was launched during the pass; the DLL was installed with RimWorld closed.

## Implemented

| Report | Change and practical limit |
|---|---|
| Constructed walls indistinguishable from natural rock | The `build` layer draws `#` natural rock, `S` smoothed natural rock wall, `:` ore vein, `H` constructed wall, from the def name in `home/get_cells_plus`. A mined face and a built wall can no longer read alike. Glyphs are in the legend and PLAYBOOK; the wrong reads of x117..119 z137 were not re-run. |
| `map.py` draws every ore vein as `%` | Terrain ores print `s` steel, `k` compacted machinery, `K` spacer machinery, `p` plasteel, `g` gold, `l` silver, `r` uranium, `j` jade; an unlisted ore stays `%` and is named in the footer. Components are now visible on the map without `order.py menu` per cell. |
| `--layers <typo>` dumps seven layers, then errors | Layer names validate before anything is drawn: unknown names exit 2 and print the valid set. `build` aliases buildings, furniture, beds, walls, doors. |
| `map.py --legend` prints the legend instead of the map | `--legend` prints the map and then the key; `--legend-only` prints the key alone and makes no bridge call. |
| `map.py` sizes read positional numbers as an opposite corner | Positional `x z WIDTH HEIGHT` now matches `act.py`. `--size W` or `--size W H` sizes around a centre; the old `--rect` is `--corner` and is the only opposite-corner form. An empty window is refused with exit 2 instead of clamping to nothing. |
| `map.py --layers items` was believed unable to see plants | It always could; the drawing is now documented — `b` wild food, `h` healroot, `c` sown crop, `t`/`T` tree, `"` other, with a species census. |
| `act.py hunt` rejects the ids `pawns.py --animals` prints | The id is read out of the nested `animals`/`settings` blocks, so `Ibex404123` and `Thing_Ibex404123` both resolve, and the disambiguation error prints the real id instead of `None`. Filed twice; the live duplicate-animal case still needs one confirming designation. |
| `act.py apply "Hunt"` refuses an already-designated animal | A standing Hunt designation reads as `NO-OP`, exit 0, without calling the designator. "Must designate huntable animals" now means not huntable. |
| Floor designation ambiguity (`sandstone tile` accepted dry, refused with `--do`) | The bridge lists a `Designator_Dropdown` row beside its active child under the same label. Resolution prefers rows with `supportsCellApplication`, in one shared path used by dry-run, `--do` and `labels`, so the three no longer disagree. |
| `act.py` and `map.py` size conventions collide | `act.py` accepts positional `label x z w h`, `--width`/`--height`, or `--size W H`, one spelling per call, and refuses a mixture with a real message. `map.py` matches the positional form. |
| `act.py apply "Mine"` silently re-armed a cancelled fort-breaching cell | An area apply reports `N cell(s) newly designated` and warns `!! touches built structure at x,z (label)` for any cell on or beside a building or blueprint. It is a warning, not a refusal; `--no-preview` skips it, and above 1024 cells the output says it did not look. |
| No unforbid route findable | New `act.py allow x z [w h]`, alias `unforbid`, dry by default with `--do`. It drives the Allow gizmo (`click_cell` → `list_selected_gizmos` → `execute_gizmo`) and re-reads afterwards, so `still forbidden` is measured, not assumed. `apply "Forbid"` on already-forbidden items reports a no-op. The gizmo path needs one live unforbid to confirm. |
| The tame refusal names the wrong animal | New `act.py tame <animal>` prints the target's ThingID and names other animals standing in the same cell, so a refusal naming a neighbour is legible. Animals still move between read and designation; area designation remains the reliable method. |
| `Deconstruct` / `Cancel` claimed by ~10 categories | Both resolve to Orders and Structure without `--category`. A genuine collision names the `--category` value to pass. |
| `inv.py components` returns 0 kinds | Matching is a case-insensitive substring against the singular label; `components` is retried as `component` and says so, and a miss prints the nearest labels. `food` is documented as a category rather than a word. |
| `inv.py` prints ThingIDs `order.py` then rejects | `[Thing_X]` was always a valid id; the stack is what expires when hauling absorbs it or a pawn eats it. `target_not_found` one call later now reads as stale: re-read, or address the cell as `Steel@133,143`. A `{held}` item is not spawned and no id reaches it. |
| Corpse freshness not shown per row | Corpse positions carry `{FRESH}` / `{SKELETON}` / `{2 SKELETON}` on each row, not summed across stacks. |
| `pawns.py <name> --work` / `--bio` ignore the name | A bare name filters every block through the server-side `nameFilter`; a miss lists the colonists instead of printing all of them. |
| `pawns.py set --work` takes internal WorkTypeDefs only | Displayed labels (Artistic, Animals, Bed rest, …) are accepted alongside defNames; a miss lists the valid names. |
| `build.py <wrong def>` returns `FAILED: None` | The bridge's own reason is printed, plus aliases and up to eight near-misses (`Conduit` → `PowerConduit`, `SculptorsTable` → `TableSculpting`). |
| `buildings.py gizmos` clears the selection it just printed | New `buildings.py gizmo <thing> "<label>" [--do]` selects, lists and fires in one process; the dry run prints the bar. `gizmos` remains read-only. This is also the Uninstall / Reinstall route for turrets and furniture. |
| Install/reinstall blueprints invisible to `--pending` | They list as `reinstall of <label>`. |
| `buildings.py --inspect <bench>` prints "none matched" then lists the bench | A match filter no longer prints empty-section noise for sections it does not apply to. |
| Phantom `pendingMissingResources` / `!! SHORT` | `!! SHORT` now means colony-usable stock — unforbidden, on the map — is below what is owed; otherwise the line reads `not yet delivered (N available in colony)`. A blueprint owes its full cost until the first haul lands. The rolled-up block prints usable / on map / in storage separately. The wording is corrected; whether the original 51-blueprint reading is fully explained needs a live repeat. |
| `bills.py recipes` refuses bare `x,z` its own error advertises | `bills.py recipes x,z` is accepted. |
| Pemmican bill reads "until you have 40 (have 0)" with 98 in existence | Target-count bills print `have 0 in counted storage (58 stored, 98 total)`. The first number is the game's own zone-scoped count, and the disagreement is now visible rather than wrong. |
| World map unreachable by any route | New companion `home/world` and `world.py`: biome, hilliness, elevation, rainfall, longitude/latitude, seasonal and average temperature, growing period, and settlements within N tiles with faction, relation and distance. `world.py --show 8` reveals the planet for eight seconds and hides it. The World tab is a MainButtonDef toggle and still cannot be opened as a tab; `ui.py --surfaces` now lists the bottom tab bar and marks it `TOGGLE`. Live viewer-facing use is unverified. |
| `order.py resolve` / `haul` print "ORDER VERIFIED" and issue nothing | Reads and dry runs print `DRY RUN - would be accepted (nothing issued)`. A real order prints `ORDER ISSUED` plus a job read-back; `ORDER ISSUED` with `!! NO JOB` is flagged as unconfirmed. |
| `order.py work <wall>` refuses while `menu` lists the option | New `order.py force <pawn> <target> ["<substring>"] [--do]`, alias `menu-do`: it runs the matching float-menu option, or the single `Prioritize ...` when there is one. `order.py work` still targets bill givers only, and its refusal points at `force`. Needs a live forced job to confirm the read-back. |
| `order.py menu` on a cell can issue a click | `menu` uses `open_context_menu` only and never a raw click. `NO MENU OPENED` means RimWorld would take the direct action for that click; `force` is the deliberate way to take it. |
| `order.py menu` target forms inconsistent | Bare `140 152`, `"140,152"`, `DefName@x,z` (blueprints included) and a thingId all resolve, the thingId to x/z first. The resolved pawn id is printed. |
| Float-menu labels truncate mid-word | The truncation was ours and is removed; refusal reasons print in full. |
| `cam.py` has no zoom and `--help` prints camera state | `--help` prints usage. `cam.py zoom <rootSize>` and `cam.py go x z --zoom n` set it. Root size is a zoom level and smaller is closer: 11–15 close, 24–35 colony view, 45–60 wide (60 is 45% of the map), ~127 the whole map. |
| `zones.py` cannot set a growing zone's crop | New `op=crop`: `zones.py crop <zone|x,z> Plant_Rice --do`. `plantDef: null` means the crop was never set. Untested against a live zone. |
| `click_cell` on a corpse over a stockpile selects the zone silently | `ui.py --selection`, and a `selected:` line after `order.py` pawn selection, name what was actually selected. Passing `targetId` remains the way to choose; the ambiguity is now visible rather than hidden. |
| `tools\ask.py` crashes on an emoji answer | Output is printed as UTF-8 with replacement and emoji are sanitised for Tk, so an answer reaches stdout instead of exit 1. |
| `play.py start` failed on `WinError 5` but reached the game | `game_session.write` retries `PermissionError` with a pid-unique temp name. `play.py start` commits owner and binding before the game call, and on a write failure pauses the game and prints `Recover with: python play.py pause then python play.py start`. The retry path needs a live write collision to confirm. |
| `FORCE_PAUSED` with no modal to answer | `Start` refuses only for a named long event or a real `forcePause` window; a merely paused TickManager just starts. A refusal detail with no window says `no force-pausing window found; clock was paused by TimeSpeed…`, so no dialog hunt follows. Confirmed offline only. |
| Lookout read the Learning Helper panel as the alerts list | The prompt states that alerts are the right-edge column and that the top-right Learning Helper panel is tutorial topics and never a lead. |
| Eight wrong Lookout leads, none useful | The prompt bans inferring "collapsed/downed" from posture and bans "cluttered/unfinished storeroom" unless items lie on open floor outside storage; zero leads is stated as normal; reports mark `SEEN:` against `EVIDENCE:`. Each lead is to be checked against `alerts.py`, `pawns.py --health` and `status.py --brief` by the reader. Whether false positives actually drop needs live observation. |
| `hands.md` ends turns at 2:30 against a six-minute budget | `hands.md` now states that a turn is six minutes and overrides the fork boilerplate "execute one directive then stop". A finished brief does not end the turn. |
| Hands has no clock and invents one | `hands.md` says Hands has no clock and must wait for the hook's five-minute warning rather than estimating elapsed time. PLAYBOOK's turn loop says the same. |
| A message from M mid-turn treated as the turn | A human message packet to Hands ends with "(This is work inside the current turn, not the turn itself. Do it, then continue the brief until the turn budget reminder arrives.)" |
| M's four methods filed as "not bugs" | Area designation, right-click force work, Uninstall/Reinstall of turrets and furniture, and components mined from compacted machinery are now PLAYBOOK practical tips, not bug lines. |

## Remaining or intentionally unchanged

- `open_main_tab` null-refs on a toggle-type main tab. The fault is in the upstream bridge; `world.py` reads the tile instead, and the World tab still cannot be opened.
- `rimworld/take_screenshot` returning `sourcePath: null`, and `rimworld/get_trade_sheet` returning a non-JSON string, are upstream. The screenshot file is still found by timestamp in `screenshotFolder`.
- The Lookout is a blind sandboxed process and cannot run instruments. Its leads can be labelled and constrained but not self-verified; verification lands on the reader.
- The vanished 428 pemmican and the occupied bed cell 113,139 remain historical. No explanation was invented for either.
- Critical-alert wake, the trade `restage_mismatch` refusal and ground tending still await live conditions.
- `rimworld/open_context_menu` ignoring `targetId` is worked around by passing x/z; no instrument yields colonist ThingIDs.

## Validation and installation

Instrument tests 275 → 491 OK. Companion source-contract checks 45 → 59 OK. `contract_test.py --offline` reports 0 suspects. The Release build produces 0 warnings and 0 errors. These are offline tests and do not establish live RimWorld behaviour.

Installed with RimWorld closed:

- DLL: 1,024,000 bytes, SHA-256 `70b47d5efc6582aad604fac5d7abb144aa9a9d4b229efb02d25f0105a647c3ef`
- Build copy: `companion/artifacts/bugfix-day67/`
- Previous installed DLL backup: `companion/artifacts/installed-backup-day67-20260906-175110/`

The next game launch loads the installed changes.
