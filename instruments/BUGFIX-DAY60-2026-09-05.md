# Day 55–60 bug pass — 2026-09-05

Astra with two Sol agents. Scope: the latest 14-turn report in BUGS.md, with turn ownership and false game-state reports first. Existing unrelated workspace edits and saves were preserved. No game was launched or save written during this pass.

## Implemented

| Report | Change and practical limit |
|---|---|
| False fork handback / two agents in one game | `hands-check` uses the claimed agent's native SubagentStop completion token, never `hands-last.md` mtime. Starting another turn or publishing Core handback while that token is open is refused. An old token remains unresolved; age never proves exit. Explicit `hands-release` does not pretend the caller has stopped. |
| Intermediate handback revisions | Intermediate writes cannot end a turn. Hands is instructed to write its report once, immediately before final return. Core cannot publish it as a completed turn until native stop. |
| Exit blocked by `runtime_hook.py park` | The native stop handler pauses/releases and returns; it no longer waits for an event or rejects the final response. The legacy command name `park` remains for settings compatibility. Normal return and forced TaskStop still need a native live smoke test. |
| Pull-only six-minute budget | Active Hands receives a five-minute warning, an independent six-minute overdue notice, then reminders roughly every 45 seconds showing elapsed time and overage. Delivery uses tool boundaries, including failed calls; cadence persists across hook processes. Scouts do not receive it. Hands must self-manage six minutes; a long tool call cannot receive a warning until it returns. No claim of a hard wall-clock interrupt. |
| `inv.py food` empty with food present | New companion `category: food` uses `ThingDef.IsNutritionGivingIngestible`, including held stock and modded nutrition-bearing defs. This category includes animal feed and nutrition-bearing drugs, not just colonist meals. Older DLLs that silently coerce the category are refused. |
| Hunting predator makes time unstartable | Exact `--ignore-hostile <thingId>` acknowledgment now also covers that predator's proximity guard. Other animals remain guarded. PLAYBOOK also explains the existing `combat.py begin` / orders / `combat.py advance 5` fallback. |
| `serviceAlive: false`, speed refused | `play_service.pid_alive` used `os.kill(pid, 0)`, which can call TerminateProcess on Windows. It now uses the non-destructive native process identity reader. A real disposable child survives repeated probes in a regression test. |
| Unnamed force-pause | Companion refusal/event now names the force-pausing window classes and gives `ui.py` as the visible title/button read. It does not automatically dismiss research or other dialogs. |
| `ui.py <surface> click` silent no-op | Added CLI click dispatch, including unscoped `ui.py click "text"`. The import API remains text first, `surface=` keyword. |
| Hunt rejects printed animal IDs | Resolves nested `animals.thingId`, including short/full Thing_ forms. Each designation refreshes the exact animal, including every `--all` member; the exact stale-cell refusal gets up to three refresh attempts. Two calls still leave a movement race; this is mitigated, not proven eliminated. |
| Blueprint wood `have 0` | Output now says materials **delivered** to the site and separately reports colony-available stock. `have` was site delivery, not global inventory. |
| Pawn settings watch freezes play | Decorative main tabs that would force-pause an already running game are skipped, with an explanation; the settings write still applies. Existing user-open tabs are respected. |
| `--roster --work` loses work | Both blocks print from the same pawn read. |
| Bill list falsely empty | Building census now distinguishes an unreadable bill stack from an empty stack and excludes unreadable stacks from empty-queue attention counts. It also matches BillsTool's PotentialBillGiver discovery. The exact historical butcher-spot 0→1 observation remains unproven. |
| Trade forbidden-stock warning wrong | Removed the unconditional claim that all bought goods are forbidden until caravan departure; output directs callers to read actual ownership/flags/location. |
| Inventory cannot see fire | Literal `fire` widens the census to `category: all`. Alerts remain the direct fire-alert read. |
| Status disagrees with predator guard | Both board and brief show nearby non-player hunts excluded from colony-target counts but caught by the 40-cell guard. No longer silently looks all-clear. |
| Brief letter count counts buttons | Brief now counts letters with actual choice text, not all Close/Jump buttons. The raw companion `letterChoiceCount` retains its older button-total semantics. |
| CLI architect apply writes immediately | `act.py apply` now previews by default, with explicit `--do`. Updated PLAYBOOK's haul example. The underlying stock designator API can still change selection in a dry run; no claim of zero UI side effects. |
| Camera-only letter cannot clear | Added explicit `letters.py dismiss <id>` using the game's right-click eligibility. No dialog or quest acceptance is invented. Existing CHOICE classification for View quest is retained conservatively. |
| Routine notification pause storms | Exact StandardLetter neutral/positive announcements and neutral/positive/historical messages are delivered as non-stopping `notification_new` events. Negative, unknown and decision notifications still stop. No text-based guesses about urgency. |
| Stale-letter / blind Lookout false alarms | Prompt explicitly treats letters as possibly historical, distinguishes evidence from inference, and permits zero leads instead of requiring 3–6. Existing verification remains. Reduction in false positives needs live observation. |
| Save / stuck clock / tabs / schedules docs | PLAYBOOK now documents mid-session `saveName`, valid blocked-clock handback, non-toggle `close_main_tab`, A/W/J/S schedule letters, camera pinning, and safe orphan recovery. Parking instructions removed from active manuals. |

## Remaining or intentionally unchanged

- A watched trade still opens a real force-pausing Dialog_Trade, which ends supervision. Output now explicitly tells the player to check status and restart after handling the stop. Automatic supervised resumption across a trade was not implemented; `--no-watch` avoids the decorative dialog.
- The original occupied bed cell 113,139 and the missing 428 pemmican require the historical save/transaction evidence. No new explanation was invented.
- The exact butcher-spot discrepancy, moving-animal designation race, live predator acknowledgment, real modal/watch behavior, routine notification routing, and native normal/forced fork return need the next live verification pass.
- Previously listed trader restaging, ground tending, and Critical alert delivery still require suitable live conditions. Old “wake parked Hands” tests are superseded: Hands now returns, and between-turn events belong to Core.
- Gear-tab job timing and deliberate unforbidding on equip remain existing behavior. `open_main_tab` is an opener, not a toggle. Camera home was already pinned during the reported session.
- `PLAY REFUSED` exit 0 remains unconfirmed; the wrapper returns nonzero on refusal.

## Validation and installation

All 275 instrument tests pass (including the recurring-reminder follow-up), companion's 45 source-contract checks pass, and the offline tool contract check passes. The Release DLL builds with zero warnings/errors. These are offline tests; source-contract checks do not establish live RimWorld behavior.

Installed with RimWorld closed:

- DLL: `C:\Program Files (x86)\Steam\steamapps\common\RimWorld\BridgeTools\HomeBridge\HomeBridge.BridgeTools.dll`
- Build: `companion/artifacts/bugfix-day60/HomeBridge.BridgeTools.dll`
- SHA-256: `9F96C52ACF47C1B47F19A9FB3C0CA53B0B075AFFD7A9BDBCE16F46364B9C47F7`
- Previous DLL backup: `companion/artifacts/installed-backup-day60-20260905-202841/HomeBridge.BridgeTools.dll`

The next game launch loads the installed changes. No commit was made; changes remain reviewable in the shared workspace.
